# There is no local user, only a customer. And no display manager: the seat
# is claimed by the login on tty1, which is what the compositor needs.
[Service]
ExecStart=
ExecStart=-/sbin/agetty --autologin customer --noclear %I $TERM
